# DODAM AI 서비스 설정
# 실제 값은 .env 파일에 넣고 절대 커밋하지 말 것

# OpenAI 키 (없으면 휴리스틱 폴백으로 동작, 있으면 LLM 사용)
OPENAI_API_KEY=sk-...

# 이 서비스가 뜰 포트
AI_PORT=8000

# (선택) 백엔드와 공유하는 인증키. 설정하면 백엔드 요청 헤더도 대조. 비워두면 인증 없음.
AI_API_KEY=
